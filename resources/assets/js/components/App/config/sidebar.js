import colors from './colors'

export default {
  activeBackgroundColor: colors.darkBackgroundDimmed,
  activeActiveBackgroundColor: "rgb(31, 34, 45)",
  activeActiveColor: "white",
  activeWidth: 16,
  
  mainActiveBackgroundColor: colors.primary,
  mainActiveFontColor: "white",
  mainBackgroundColor: colors.darkBackground,
  mainInactiveFontColor: colors.inactiveFontColor,
  mainWidth: 8,
}